# JuniCampainha




create  a file names 


credentials.json

with tha folowing format:

{
    "database credentials": {
        "host": "HOST_URL",
		"user": "DB_USER",
        "password": "DB_PASSWORD"
    }
}